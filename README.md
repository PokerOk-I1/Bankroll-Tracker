# Трекер банкролла с анализом графика выигрыша

Bankroll-Tracker `bankroll.py` — это консольный Python-скрипт для учёта и анализа покерного банкролла, с упором на покер-рум PokerOK, но при этом поддерживающий и другие руумы.

Проект позволяет:
- вести базу результатов турниров и кэш-сессий в формате CSV  
- считать ключевые показатели (профит, ROI, ITM, даунсвинг и др.)  
- строить график кумулятивного профита  
- импортировать результаты из CSV (например, выгрузки из Smart Results / ручных таблиц)

---

## 📦 Возможности

- ✅ Хранение банкролла в одном файле `bankroll_data.csv`  
- ✅ Добавление сессий/турниров вручную (`add`)  
- ✅ Импорт из CSV (`import`)  
- ✅ Сводная статистика по выбранному периоду и фильтрам (`summary`)
- ✅ Построение и сохранение графика кумулятивного профита (`graph`)
- ✅ Просмотр истории сыгранных игр (`history`)
- ✅ Очистка базы с подтверждением (`reset`)
- ✅ Простая конфигурация и понятное CLI

---

## 🔧 Требования

- **Python**: 3.9+
- Рекомендуется создание отдельного виртуального окружения.

### Зависимости

Установите зависимости через `pip`:

```bash
pip install pandas matplotlib
````

---

## 🚀 Установка

1. Клонировать репозиторий:

```bash
git clone https://github.com/PokerOk-I1/Bankroll-Tracker.git
cd Bankroll-Tracker
```

2. Установить зависимости:

```bash
pip install -r requirements.txt
```

> Если файла `requirements.txt` нет — можно просто установить:
>
> ```bash
> pip install pandas matplotlib
> ```

3. Убедиться, что скрипт исполняемый:

```bash
python bankroll.py --help
```

---

## 📁 Структура данных

Основной файл данных: **`bankroll_data.csv`** (создаётся автоматически при добавлении/импорте).

Каждая строка — **один турнир или сессия**.

Поля:

| Поле          | Описание                                          |
| ------------- | ------------------------------------------------- |
| `id`          | Уникальный идентификатор записи (UUID)            |
| `date`        | Дата/время (`YYYY-MM-DD` или `YYYY-MM-DD HH:MM`)  |
| `room`        | Рум (по умолчанию `PokerOK`)                      |
| `game_type`   | Тип игры (`MTT`, `Cash`, `Spin&Go`, `SNG` и т.п.) |
| `limit`       | Лимит / бай-ин (напр. `NL10`, `2$`)               |
| `format`      | Формат (`Holdem`, `Omaha`, `Short Deck` и т.п.)   |
| `buyin`       | Бай-ин без рейка                                  |
| `fee`         | Рейк                                              |
| `rebuys`      | Количество ребаев                                 |
| `addons`      | Количество аддонов                                |
| `total_buyin` | Общий вложенный бай-ин (с ребаями и аддонами)     |
| `cashout`     | Выплата/выигрыш (0, если ничего не выиграли)      |
| `result`      | Профит = `cashout - total_buyin`                  |
| `currency`    | Валюта (по умолчанию `USD`)                       |
| `tags`        | Теги через запятую (`evening,reg,deepstack`)      |
| `notes`       | Заметка (название турнира, комментарии и т.п.)    |

Если при добавлении или импорте:

* `total_buyin` не указан — он автоматически считается по формуле
  `total_buyin = (buyin + fee) * (1 + rebuys + addons)`
* `result` не указан — считается как
  `result = cashout - total_buyin`

---

## 🧷 Общий синтаксис CLI

```bash
python bankroll.py <команда> [опции]
```

Посмотреть помощь:

```bash
python bankroll.py --help
python bankroll.py summary --help
```

Доступные команды:

* `add` — добавить одну запись
* `import` — импортировать CSV
* `summary` — сводка статистики
* `graph` — график кумулятивного профита
* `history` — история игр
* `reset` — очистка базы (удаление CSV)
* `config` — показать текущие настройки

Общий флаг:

* `--db-path` — путь к базе (по умолчанию `bankroll_data.csv`)
* `--verbose` — подробный отладочный вывод

---

## 🧩 Общие фильтры

Для команд `summary`, `graph`, `history` поддерживаются одинаковые фильтры:

* `--from-date YYYY-MM-DD` — начиная с даты (включительно)
* `--to-date YYYY-MM-DD` — до даты (включительно)
* `--room` — по руму (например, `PokerOK`)
* `--game-type` — по типу игры (можно несколько через запятую, напр. `MTT,Cash`)
* `--format` — по формату (`Holdem`, `Omaha` и т.п.)
* `--limit` — подстрока лимита/бай-ина (напр. `"2$"` или `"NL10"`)
* `--tag` — подстрока в поле `tags` (например, `reg`, `evening`)

Фильтры можно комбинировать.

---

## ✏️ Команда `add` — добавить запись

Ручное добавление одного турнира / одной сессии.

**Пример:**

```bash
python bankroll.py add \
  --date "2025-12-05" \
  --game-type MTT \
  --limit "2$" \
  --format Holdem \
  --buyin 2 \
  --fee 0.2 \
  --rebuys 1 \
  --addons 0 \
  --cashout 15 \
  --currency USD \
  --tags "evening,reg" \
  --notes "Bounty Hunters $2.10"
```

Основные параметры:

* `--date` — дата/время (`YYYY-MM-DD` или `YYYY-MM-DD HH:MM`, по умолчанию — сейчас)
* `--room` — рум (по умолчанию `PokerOK`)
* `--game-type` — тип игры (`MTT`, `Cash`, `Spin&Go`, `SNG`…)
* `--limit` — лимит/бай-ин (`NL10`, `2$`…)
* `--format` — формат (`Holdem`, `Omaha`…)
* `--buyin` — бай-ин без рейка
* `--fee` — рейк
* `--rebuys` — число ребаев
* `--addons` — число аддонов
* `--cashout` — выплата
* `--total-buyin` — можно задать вручную (иначе считается автоматически)
* `--result` — можно задать вручную (иначе считается как cashout - total_buyin)
* `--currency` — валюту
* `--tags` — теги
* `--notes` — заметка

После добавления скрипт выведет что-то вроде:

```text
[OK] Добавлена запись: profit = +12.80 USD (cashout=15.00, total_buyin=2.20)
```

---

## 📥 Команда `import` — импорт CSV

Импортирует сразу много записей из CSV-файла.

**Пример:**

```bash
python bankroll.py import --file pokerok_results.csv
```

Скрипт пытается автоматически сопоставить колонки импортируемого файла с внутренней структурой, используя возможные синонимы:

* `date`: `date`, `Date`, `datetime`, `time`
* `buyin`: `buyin`, `buy_in`, `buy-in`, `bi`
* `cashout`: `cashout`, `payout`, `prize`, `winnings`, `win`
* `fee`: `fee`, `rake`, `commission`
* и т.д.

**Обязательные поля** в CSV (с учётом синонимов):

* дата (`date`)
* бай-ин (`buyin`)
* выплата (`cashout` / `prize` / `winnings` и др.)

При успешном импорте выводится:

```text
Импортировано: 125 записей
Пропущено (ошибки): 3
Итоговый профит по импортированным: +87.40 USD
```

---

## 📊 Команда `summary` — сводка статистики

Показывает ключевую статистику по выборке.

**Пример:**

```bash
python bankroll.py summary --from-date 2025-12-01 --game-type MTT --room PokerOK
```

Выводит примерно:

```text
Период: 2025-12-01 — 2025-12-06
Фильтры: game_type=MTT, room=PokerOK

Общее число игр: 42
Дней с игрой: 5

Общий вложенный бай-ин (total_buyin): 210.50 USD
Общая выплата (cashout):           278.90 USD
Итоговый профит:                   +68.40 USD

ROI:              32.5 %
ITM (выплата > 0): 40.5 %
Средний профит на игру: +1.63 USD

Лучший день:  2025-12-03 (+35.70 USD)
Худший день:  2025-12-04 (-18.20 USD)
Макс. даунсвинг за период: -24.10 USD
```

Считаются:

* Суммарный профит
* ROI
* ITM (процент записей с `cashout > 0`)
* Средний профит на игру
* Лучший и худший день
* Максимальный даунсвинг по кумулятивному графику

---

## 📈 Команда `graph` — график кумулятивного профита

Строит график изменения профита по дням (кумулятивно) и сохраняет в PNG.

**Пример:**

```bash
python bankroll.py graph \
  --from-date 2025-11-01 \
  --to-date 2025-12-06 \
  --game-type MTT \
  --output bankroll_nov_dec.png
```

Опции:

* `--output` — имя PNG-файла (по умолчанию `bankroll_graph.png`)
* `--show` — дополнительно открыть график в окне (matplotlib `plt.show()`)

По оси X — даты, по оси Y — кумулятивный профит.

---

## 📜 Команда `history` — история игр

Показывает таблицу с сыгранными турнирами/сессиями с учётом фильтров.

**Пример:**

```bash
python bankroll.py history \
  --from-date 2025-12-01 \
  --game-type MTT \
  --limit "2$" \
  --limit-rows 20
```

Вывод:

```text
Дата         Тип  Лимит  Бай-ин  Рейк  Ребаи  Аддоны  Cashout  Результат  Валюта  Заметка
-----------------------------------------------------------------------------------------
2025-12-01   MTT  2$     2.20    0.20  0      0       0.00     -2.20      USD     Bounty Hunters $2.10
2025-12-01   MTT  2$     2.20    0.20  1      0       15.00    +12.80     USD     Bounty Hunters $2.10
...

Итого по выборке: профит = +68.40 USD (игр: 42)
```

Опция:

* `--limit-rows` — ограничить количество выводимых строк

---

## 🧨 Команда `reset` — очистка базы

Удаляет файл базы данных (CSV) **после подтверждения**.

```bash
python bankroll.py reset
```

Скрипт спросит:

```text
ВНИМАНИЕ: будет удалён файл bankroll_data.csv. Продолжить? (yes/no):
```

Только при ответе `yes` файл будет удалён.

---

## ⚙️ Команда `config` — текущие настройки

Показывает базовую конфигурацию:

```bash
python bankroll.py config
```

Пример вывода:

```text
Текущая конфигурация:
  Путь к базе данных: /полный/путь/к/bankroll_data.csv
  Дефолтный рум: PokerOK
  Дефолтная валюта: USD
```

---

## 🤝 Контрибьюция

PR и идеи приветствуются!
Возможные направления развития:

* поддержка других румов (PokerStars, Winamax и др.)
* конвертация валют по курсу (автообновление)
* экспорт отчётов в PDF/HTML
* простенький веб-интерфейс поверх существующего CSV
* поддержка разных форматов импорта (XLSX, JSON)

Если хотите предложить изменения:

1. Сделайте форк репозитория
2. Создайте ветку `feature/...` или `fix/...`
3. Отправьте Pull Request

---

## 📄 Лицензия

Проект распространяется по лицензии **MIT**. Вы можете свободно использовать, изменять и распространять код, сохраняя указание авторства и лицензию. Подробнее см. файл [`LICENSE`](./LICENSE) в репозитории.
